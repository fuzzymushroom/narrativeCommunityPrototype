navbar
	name
	edit					(self only)
	follow/unfollow			(others only)
"you"
	active scene
		edit				(self only)
	scenelist
		load
		reorder				(self only)
		add					(self only)
		delete				(self only)
	about the author 		(writers only)
stories
	authored				(writers only)
		scroll
		launch
		rose count
		reorder				(self only)
	favorites
		scroll
		launch
		rose count
		remove				(self only)
		add (manually)		(self only)
		reorder				(self only)
people
	following
		list
			open
			unfollow		(self only)
		page
	followers				(if not null)
		post				(self only)
		list				
			name
			icon
			roses given
		page
		Q&A